"use client";
import { useEffect, useState } from "react";
import Shell from "@/components/Shell";
type Video = { video_id: string; product_id: string; status: string; dias_restantes: number | null; total_ganho_criadora: number };
export default function MeusVideos() { const [videos, setVideos] = useState<Video[]>([]); const [error, setError] = useState(""); useEffect(() => { fetch("/api/videos/demo-creator").then(async r => { const b = await r.json(); if (!r.ok) throw Error(b.error); setVideos(b.videos ?? []); }).catch(e => setError(e.message)); }, []); return <Shell><div className="page-head"><div><p className="eyebrow">Sua produção</p><h1>Meus vídeos</h1><p>Cada vídeo tem seu próprio link e janela de comissão.</p></div></div><div className="card table-wrap">{error ? <p className="muted">{error}</p> : <table className="data-table"><thead><tr><th>Produto</th><th>Status</th><th>Dias restantes</th><th>Ganho</th><th>Link</th></tr></thead><tbody>{videos.map(v => <tr key={v.video_id}><td>{v.product_id}</td><td>{v.status}</td><td>{v.dias_restantes ?? "—"}</td><td className="money">R$ {(v.total_ganho_criadora ?? 0).toFixed(2).replace(".", ",")}</td><td><a href={`/v/${v.video_id}`}>Abrir</a></td></tr>)}</tbody></table>}</div></Shell>; }
