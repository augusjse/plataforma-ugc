"use client";
import { useEffect, useState } from "react";
import Shell from "@/components/Shell";
type Video = { video_id: string; creator_id: string; product_id: string; status: string; total_sales: number; total_ganho_criadora: number };
export default function AdminVideos() { const [videos, setVideos] = useState<Video[]>([]); const [filter, setFilter] = useState(""); useEffect(() => { fetch("/api/videos").then(r => r.json()).then(b => setVideos(b.videos ?? [])); }, []); const rows = filter ? videos.filter(v => v.status === filter) : videos; return <Shell admin><div className="page-head"><div><p className="eyebrow">Operação</p><h1>Vídeos</h1><p>Janelas, vendas e repasses por vídeo.</p></div></div><div className="filter-row"><select value={filter} onChange={e => setFilter(e.target.value)}><option value="">Todos os status</option><option value="aguardando_primeira_venda">Aguardando primeira venda</option><option value="ativo">Ativo</option><option value="encerrado">Encerrado</option></select></div><div className="card table-wrap"><table className="data-table"><thead><tr><th>Criadora</th><th>Produto</th><th>Status</th><th>Vendas</th><th>Comissão paga</th></tr></thead><tbody>{rows.map(v => <tr key={v.video_id}><td>{v.creator_id}</td><td>{v.product_id}</td><td>{v.status}</td><td>{v.total_sales}</td><td className="money">R$ {(v.total_ganho_criadora ?? 0).toFixed(2).replace(".", ",")}</td></tr>)}</tbody></table></div></Shell>; }
